{
  "name": "ml.box",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 4173",
    "og": "uv run --with 'fonttools[woff]' python scripts/make-og-fonts.py && node scripts/gen-og.mjs",
    "smoke": "node scripts/smoke.mjs"
  },
  "devDependencies": {
    "@resvg/resvg-js": "^2.6.2",
    "puppeteer-core": "^25.3.0",
    "vite": "^6.0.0",
    "wrangler": "^3.90.0"
  }
}
